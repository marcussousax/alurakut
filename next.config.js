module.exports = {
    reactStrictMode: true,
    env: {
        DATOCMS_TOKEN: process.env.DATOCMS_TOKEN,
    },
    images: {
        domains: ['avatars.githubusercontent.com', 'www.alura.com.br', 'alurakut.vercel.app', 'github.com', 'picsum.photos']
    }
}
